<div>
	<h1>Hola {{user.name}} {{user.last_name}}, que gusto verte! asdasdasd</h1>

	{{#if isAdmin}}
		<h3>es admin</h3>
		{{#each food}}
			<div>
				<p>{{this.name}}</p>
				<p>{{this.price}}</p>
			</div>
		{{/each}}
	{{else}}
		<h3>es user, no puede ver los productos</h3>
	{{/if}}
</div>
<script src="/js/index.js"></script>